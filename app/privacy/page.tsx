import type { Metadata } from "next";
import { PageHero } from "@/components/content/page-hero";

export const metadata: Metadata = { title: "Privacy", description: "Local development privacy notice template for HOS AI forms." };

export default function PrivacyPage() {
  return <><PageHero eyebrow="Privacy" title="A clear boundary around contact data." description="This local-development version documents the intended collection rules. It must be completed with actual provider and publisher information before any public form is opened." badge="Requires production review" /><article className="mx-auto max-w-3xl space-y-8 px-5 pb-20 text-sm leading-7 text-[var(--muted-foreground)] lg:px-8"><section><h2 className="text-lg font-semibold text-[var(--foreground)]">What forms collect</h2><p className="mt-2">Participation and contact forms collect professional contact details, organisation context, a participation interest and concise free text necessary to qualify the enquiry. They do not request guest data, credentials, API keys, exports, message content or confidential commercial information.</p></section><section><h2 className="text-lg font-semibold text-[var(--foreground)]">Why it is used</h2><p className="mt-2">The information is used only to qualify and respond to the requested HOS AI participation path or general inquiry. There are no advertising pixels, retargeting audiences or marketing automation in this local build.</p></section><section><h2 className="text-lg font-semibold text-[var(--foreground)]">Retention and deletion</h2><p className="mt-2">A submission is scheduled for deletion twelve months after the last exchange, unless a deletion request is received sooner or a lawful reason requires a documented exception. Contact <a className="text-[var(--accent)] underline" href="/contact">HOS AI</a> to request deletion.</p></section><section><h2 className="text-lg font-semibold text-[var(--foreground)]">Local-only implementation</h2><p className="mt-2">During local development, submissions are stored in a locally run Postgres database and acknowledgements are written to a local outbox. Hosting, database, email, analytics and anti-spam processors must be named accurately before publication.</p></section></article></>;
}
