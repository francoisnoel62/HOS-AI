<PageInProgress>

This guide will show how to use `hos replay` to see what a consumer does with each fact of a stream, to debug an adapter:

- reading the timeline column by column, and the ▲ and ▼ marks;
- `--ready`, which sets the housekeeping statuses that make a unit ready (`clean` and `inspected` by default), and `--json`;
- the most common mistake: forgetting `--manifest`, which makes every fact `undeclared_capability`.

</PageInProgress>
