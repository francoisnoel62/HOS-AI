<PageInProgress>

This guide will show how to run the checks automatically:

- the exit codes, 0, 1 and 2, and what a CI should do with each;
- `--json` output, and JUnit reports with `--junit`;
- a complete, commented GitHub Actions workflow that validates files, runs the conformance scenarios, checks a producer and warns before a manifest signature expires;
- the same for GitLab CI;
- pinning the version of the tools.

</PageInProgress>
