<PageInProgress>

This guide will show how to check what a producer publishes with `hos conformance producer`:

- why a producer is checked on what it publishes, where a consumer is checked with scenarios;
- the four checks: a valid manifest with its limitations stated; valid facts, from the right source, declared for the property; never two facts for the same source and id; and a redelivery that sends the same ids with the same content;
- how to record `recording.jsonl` and `redelivery.jsonl`, by restarting the adapter on the same data;
- why fact ids must be deterministic.

</PageInProgress>
