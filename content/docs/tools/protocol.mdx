<PageInProgress>

This page will present `hos-conformance/1`, the protocol between `hos conformance run` and the program it tests, with a sequence diagram, the input and output formats, the levels, and how the tool stops a program that takes too long on each system.

Meanwhile, the protocol is published as [PROTOCOL.md](/spec/0.1/conformance/PROTOCOL.md).

</PageInProgress>
