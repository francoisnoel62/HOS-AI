<PageInProgress>

This reference will have a section for each command and subcommand of `hos`: `validate`, `replay`, `conformance list`, `conformance run`, `conformance producer`, `manifest keygen`, `manifest sign` and `manifest verify`. Each section will give:

- the synopsis, taken from the command's own help;
- every option, with its type, its default and whether it repeats;
- the exit codes, examples, the text and JSON output, and the common errors.

Meanwhile, `hos <command> --help` lists the options of a command, and the [npm page](https://www.npmjs.com/package/@hos-ai/cli) lists the commands and exit codes.

</PageInProgress>
