<PageInProgress>

This page will explain each rule the tools report, such as `events/minimal-data` or `core/unit-status-model`, under its own heading, so that a rule id in an error leads straight to it. For each rule:

- what it means, in plain language, and why it exists;
- a real example that breaks it, from the conformance cases;
- the exact message the hos command prints;
- how to fix it;
- the section of the specification that states it.

Meanwhile, [HOS Core 0.1](/docs/core) and [HOS Events 0.1](/docs/events) state the rules.

</PageInProgress>
