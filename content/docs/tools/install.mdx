<PageInProgress>

This page will take you through installing the tools on Windows, macOS or Linux, and checking that they work:

- checking that Node.js 22 or later is installed, and what to do if it is older;
- the three ways to run the hos command, and when to use each: `npx @hos-ai/cli` to try it, `npm install -g @hos-ai/cli` to have `hos` everywhere, and `npm install --save-dev @hos-ai/cli` to pin a version in a project or a CI;
- the differences between PowerShell, the Command Prompt and macOS or Linux shells, such as `npx.cmd` and `curl.exe`;
- installing the SDK with `npm install @hos-ai/sdk`;
- company networks, proxies and internal npm registries;
- updating and uninstalling.

Meanwhile, the [npm page of the hos command](https://www.npmjs.com/package/@hos-ai/cli) covers installation, with a section for Windows.

</PageInProgress>
